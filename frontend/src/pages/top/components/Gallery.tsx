import { Image } from "../../../types/image";

//💻TODO:画像を渡す処理を後で書く。spのときは前から4-5枚スライス。pcのときは全部渡す。💻
const Gallery = () => {
  return (
    <div className="h-[90vh] w-[36vw] py-8">
      <div className="grid grid-cols-2 gap-[1px] border border-deathDarkGray">
        {/* figure */}
      </div>
    </div>
  );
};

export default Gallery;
